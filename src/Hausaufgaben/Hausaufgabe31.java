package Hausaufgaben;

public class Hausaufgabe31 {

	/*
	
	Aufgabe erfordert nur Erklärung

	void initA:
	gibt dem Array a 3 Speichermodule und füllt das 0. mit -999
	
	int get AO:
	Gibt den Wert des Arrays and 0. Stelle zurück also -999
	
	 void primPara:
	 Deklarieren eines neuen int a  und initialisieren mit dem Wert 5
	 
	 void arrrayPara:
	 Deklarieren eines neuen 1Dimensionalen Arrays a und initialiseren der 0. Stelle mit dem Wert 5
	 
	 void runMain:
	 Deklarieren eines neuen int i  und initialisieren mit dem Wert 10
	 Deklarieren eines neuen 1Dimensionalen Arrays a mit der Länge 1 und initialiseren der 0. Stelle mit dem Wert 10
	 Aufruf der Methode initA -> erstellen eines weiteren 1Dimensionalen Arrays a mit der Länge 3, 0. Stelle -999
	 SOP: Primitiver Datentyp (tab) & Ausgabe von i (also 10)
	 Aufruf Methode primPara mit Übergabe wert i -> neue int Variable a mit Wert 5
	 SOP: " ---- " + 10;
	 SOP: "Array" (tab) (tab) (tab) + Array a an 0.Stelle also 10
	 Aufruf arrayPara mit Übergabewert a -> Überschreiben der 0.Stelle mit dem Wert 5
	 SOP: " ---- " + Array a an 0.Stelle also 5
	 SOP: "Instantzvariable: --- " + Array a an 0.Stelle also -999 (Wegen Methodenaufruf getA0), da die Methode sich auf das übergeordnete a bezieht
	 
	 
	 Neue Klasse
	 MainMethode:
	 Erzeugen eines neuen 1Dimensionalen Int Arrays a
	 Aufruf der runMain Methode für das neue Array a (siehe oben) 
	
	*/
	
}
